#!/usr/bin/python3
def multiple_returns(sentence):
    my_tupple = ()
    if len(snetence) == 0:
        my_tupple = (0, "None")
    else:
        my_tupple = (len(sentence), sentence[0])
    return my_tupple
